"""LUT generators for hilbertsfc.

This package lives under `scripts/` so it is not part of the distributed
library code. Keep pure table generation logic here; keep CLI entrypoints in
`scripts/*.py`.
"""
